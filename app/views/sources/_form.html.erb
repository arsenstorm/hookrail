<%= form_with model: @source, class: "space-y-4 max-w-md" do |f| %>
  <% if @source.errors.any? %>
    <div class="mb-4 rounded-md bg-red-50 p-3 text-sm text-red-800">
      <ul class="list-disc list-inside">
        <% @source.errors.full_messages.each do |m| %><li><%= m %></li><% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= f.label :name, class: "block text-sm font-medium text-gray-700 mb-1" %>
    <%= f.text_field :name, class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
  </div>
  <section class="border-t pt-4 space-y-4">
    <div>
      <h2 class="text-sm font-semibold text-gray-700">Inbound verification</h2>
      <p class="text-xs text-gray-500 mt-1">
        Optional HMAC signature check on incoming webhooks. Leave the secret blank to disable verification
        and accept every request.
      </p>
    </div>

    <div>
      <%= f.label :verification_secret, "Signing secret", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :verification_secret, class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
    </div>

    <div>
      <%= f.label :verification_header, "Signature header", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :verification_header, class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
    </div>

    <div>
      <%= f.label :verification_algorithm, "Algorithm", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.select :verification_algorithm, %w[sha256 sha1 sha512], {},
            class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
    </div>

    <div>
      <%= f.label :verification_encoding, "Encoding", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.select :verification_encoding, %w[hex base64], {},
            class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
    </div>

    <div>
      <%= f.label :verification_header_format, "Header format", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.select :verification_header_format,
            [ [ "Entire header value", "value" ], [ "Comma-separated key=value pairs", "kv" ] ], {},
            class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
    </div>

    <div>
      <%= f.label :verification_signature_prefix, "Signature prefix", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :verification_signature_prefix, placeholder: "sha256=",
            class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
    </div>

    <div>
      <%= f.label :verification_signature_key, "Signature key", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :verification_signature_key, placeholder: "v1",
            class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
      <p class="text-xs text-gray-500 mt-1">Only used when the header format is key=value pairs.</p>
    </div>

    <div>
      <%= f.label :verification_timestamp_key, "Timestamp key", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :verification_timestamp_key, placeholder: "t",
            class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
    </div>

    <div>
      <%= f.label :verification_timestamp_header, "Timestamp header", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :verification_timestamp_header,
            class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
      <p class="text-xs text-gray-500 mt-1">Use when the timestamp arrives in its own header instead of the signature header.</p>
    </div>

    <div>
      <%= f.label :verification_payload_template, "Payload template", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :verification_payload_template, placeholder: "{body}",
            class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
      <p class="text-xs text-gray-500 mt-1">What gets signed. <code>{body}</code> for the raw body, or <code>{timestamp}.{body}</code> to bind the timestamp.</p>
    </div>

    <div>
      <%= f.label :verification_tolerance_seconds, "Tolerance (seconds)", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.number_field :verification_tolerance_seconds,
            class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
      <p class="text-xs text-gray-500 mt-1">Reject requests whose timestamp is older than this. Blank means no timestamp check.</p>
    </div>

    <details class="text-xs text-gray-600">
      <summary class="cursor-pointer text-gray-500">Example configurations</summary>
      <div class="mt-2 space-y-3">
        <div>
          <div class="font-medium text-gray-700">Signature in the whole header value</div>
          <p>Header <code>X-Hub-Signature-256</code>, format "Entire header value", prefix <code>sha256=</code>,
             algorithm sha256, encoding hex, payload <code>{body}</code>.</p>
        </div>
        <div>
          <div class="font-medium text-gray-700">Signature in key=value pairs</div>
          <p>Header <code>Stripe-Signature</code>, format "Comma-separated key=value pairs", signature key <code>v1</code>,
             timestamp key <code>t</code>, algorithm sha256, encoding hex, payload <code>{timestamp}.{body}</code>,
             tolerance <code>300</code>.</p>
        </div>
      </div>
    </details>
  </section>

  <section class="border-t pt-4 space-y-4">
    <div>
      <h2 class="text-sm font-semibold text-gray-700">Deduplication</h2>
      <p class="text-xs text-gray-500 mt-1">
        Optional. Drops repeats of the same event inside a time window. Leave the window blank to disable
        deduplication and deliver every request.
      </p>
    </div>

    <div>
      <%= f.label :dedupe_window, "Dedupe window (seconds)", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.number_field :dedupe_window, min: 1, max: 86400, placeholder: "Off",
            class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
      <p class="text-xs text-gray-500 mt-1">Blank disables deduplication. Repeats of the same event within this window are marked duplicates and never delivered.</p>
    </div>

    <div>
      <%= f.label :dedupe_key, "Identity key", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :dedupe_key, placeholder: "X-GitHub-Delivery",
            class: "w-full rounded-md border border-gray-300 px-3 py-2 text-sm" %>
      <p class="text-xs text-gray-500 mt-1">What makes two events the same: a header name, e.g. <code>X-GitHub-Delivery</code>, or a body path in dot syntax, e.g. <code>data.object.id</code>. Headers are checked first. Blank compares the SHA-256 of the raw body.</p>
    </div>
  </section>

  <%= f.submit submit_label, class: "rounded-md bg-blue-600 text-white px-3 py-1.5 text-sm hover:bg-blue-700" %>
<% end %>
