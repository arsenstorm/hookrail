<% content_for :title, "New source" %>
<div class="w-full max-w-4xl mx-auto py-16">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-semibold">New source</h1>
    <%= link_to "Dashboard", root_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
  </div>

  <%= render "form", submit_label: "Create source" %>
  <%= link_to "← Sources", sources_path, class: "text-sm text-gray-500 hover:text-gray-700 mt-4 inline-block" %>
</div>
