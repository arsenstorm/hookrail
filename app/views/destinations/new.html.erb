<% content_for :title, "New destination" %>
<div>
  <div class="mb-6 flex flex-wrap items-center justify-between gap-4">
    <h1 class="<%= ui(:heading) %>">New destination</h1>
  </div>

  <%= render "form", submit_label: "Create destination" %>
  <%= link_to "← Destinations", destinations_path, class: "#{ui(:muted_link)} mt-4 inline-block" %>
</div>
